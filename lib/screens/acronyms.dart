import 'package:flutter/material.dart';

class Acronyms extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Acronym Search")
      ),
      body: Text(
        "textPizza",
        textDirection: TextDirection.ltr,
      )
    );
  }

}